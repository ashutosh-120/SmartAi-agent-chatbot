# frontend/.env.example — Copy to .env.local and fill in values
# Used by Vite to configure API base URL

# Backend API URL
# Development (default): http://localhost:8000
# Production (Vercel → Render/Railway): https://your-backend.onrender.com
VITE_API_URL=http://localhost:8000
